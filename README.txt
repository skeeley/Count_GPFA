Code will simulate data, then perform inference on it. Uses a set of tools (in GP_fourier) to do MAP inference in the Fourier domain.


Code requires the autograd package to work: https://github.com/HIPS/autograd

